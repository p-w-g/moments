PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Viewer-role token, needed only for /dev/heroes (draft hero candidates).
# Create at sanity.io/manage → API → Tokens. Never committed, dev-only route.
SANITY_API_READ_TOKEN=
